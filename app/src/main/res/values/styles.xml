<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>
    <!-- FullScreen theme. -->
    <style name="FullScreen" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="tilStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="etStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/padding_padrao</item>
    </style>

    <style name="botao">
        <item name="android:background">@drawable/botao_personalizado</item>
        <item name="android:textSize">@dimen/botao_tamanho_texto</item>
        <item name="android:padding">@dimen/padding_padrao</item>
    </style>


    <style name="clickableElement">
        <item name="android:textColor">@color/clickable</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/padding_padrao</item>
        <item name="android:textSize">@dimen/botao_tamanho_texto</item>
    </style>

    <style name="cardStyle">
        <item name="android:padding">@dimen/padding_padrao</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_marginBottom">@dimen/dimen_bottom_card</item>
        <item name="android:layout_marginLeft">@dimen/dimen_bottom_card</item>
        <item name="android:layout_marginRight">@dimen/dimen_bottom_card</item>

    </style>

    <style name="iconStyle">
        <item name="android:layout_weight">0</item>
        <item name="android:layout_width">@dimen/dimen_icon_width</item>
        <item name="android:layout_height">@dimen/dimen_icon_height</item>
        <item name="android:clickable">true</item>
    </style>
</resources>
